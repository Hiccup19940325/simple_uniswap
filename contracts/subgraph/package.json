{
  "name": "swap",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service hiccup19940325/swap",
    "create-local": "graph create --node http://localhost:8020/ john/swap",
    "remove-local": "graph remove --node http://localhost:8020/ john/swap",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 john/swap",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.62.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
